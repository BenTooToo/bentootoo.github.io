const translations = {
  zh: {
    "name.display": "本2兔",
    "nav.home": "首页",
    "nav.projects": "项目",
    "nav.blog": "思索之屋",
    "nav.contact": "联系我",
    "more.menuLabel": "更多",
    "more.donate": "捐助我",
    "more.devlog": "开发日志",
    "donate.onlyfansRegion": "（全球）",
    "donate.afdianRegion": "（中国）",
    "hero.titleLineOne": "欢迎，我是",
    "hero.lede": "这里是本2兔灵感堆积之所",
    "card.identity": "学生 / 视频创作者 / 游戏制作人 / 独立音乐人",
    "card.email": "邮箱",
    "projects.title": "项目展示",
    "projects.lede": "他至少最近在尝试制作有趣的东西",
    "projects.emptyLabel": "开山之作",
    "projects.emptyTitle": "折棠7中同窗录",
    "projects.emptyBody": "虚拟现实解密（ARG），探索正常的班级回忆录背后的秘密……",
    "projects.emptyCta": "点击图片开始游戏 ↗",
    "projects.emptyLinkLabel": "进入《折棠7中同窗录》解谜游戏",
    "projects.towerLabel": "GMTK2026作品，主题为 Countdown",
    "projects.towerTitle": "Tower Off",
    "projects.towerBody": "第一个团队作品，动作游戏，2D，像素风格",
    "projects.towerCta": "点击前往 itch.io 开始游戏 ↗",
    "projects.towerLinkLabel": "在 itch.io 上游玩 Tower Off",
    "blog.title": "思索之屋",
    "blog.lede": "思索之屋：本2兔用哲学、音乐、制作和技术连接文章、类别与彼此的想法。",
    "blog.cardLatestOneDate": "2026-08-05 / 制作 / 音乐",
    "blog.cardLatestOneTitle": "我的理想",
    "blog.cardLatestOneBody": "名为幼稚的成熟",
    "blog.cardLatestTwoDate": "2026-08-05 / 哲学 / 技术",
    "blog.cardLatestTwoTitle": "论 AI、奇异点与共产主义",
    "blog.cardLatestTwoBody": "我们不探讨政治，我们探讨未来",
    "blog.cardHouseTitle": "踏入思索之屋",
    "contact.title": "联系我",
    "contact.lede": "缺席对于本2兔来说是短暂的休息，他一定会回复，无论你是谁。",
    "contact.email": "通过思索之屋留言",
    "contact.github": "github.com/BenTooToo",
    "contact.backTop": "回到顶部",
    "footer.note": "为 GitHub Pages 构建。"
  },
  en: {
    "name.display": "Bentootoo",
    "nav.home": "Home",
    "nav.projects": "Projects",
    "nav.blog": "House of Reflection",
    "nav.contact": "Contact",
    "more.menuLabel": "More",
    "more.donate": "Support me",
    "more.devlog": "Development log",
    "donate.onlyfansRegion": "(Global)",
    "donate.afdianRegion": "(China)",
    "hero.titleLineOne": "Welcome, I am",
    "hero.lede": "This is where Bentootoo lets inspiration pile up.",
    "card.identity": "Student / Video creator / Game maker / Independent musician",
    "card.email": "Email",
    "projects.title": "Projects",
    "projects.lede": "At least lately, he has been trying to make interesting things.",
    "projects.emptyLabel": "Opening work",
    "projects.emptyTitle": "Empty Class",
    "projects.emptyBody": "An alternate reality puzzle (ARG) about uncovering the secrets behind an ordinary class memory book.",
    "projects.emptyCta": "Click the image to play ↗",
    "projects.emptyLinkLabel": "Play Empty Class",
    "projects.towerLabel": "GMTK 2026 entry · Theme: Countdown",
    "projects.towerTitle": "Tower Off",
    "projects.towerBody": "Our first team project: a 2D pixel-art action game.",
    "projects.towerCta": "Play on itch.io ↗",
    "projects.towerLinkLabel": "Play Tower Off on itch.io",
    "blog.title": "House of Reflection",
    "blog.lede": "The House of Reflection connects Bentootoo's essays and readers' ideas through philosophy, music, making, and technology.",
    "blog.cardLatestOneDate": "2026-08-05 / Making / Music",
    "blog.cardLatestOneTitle": "My Ideal",
    "blog.cardLatestOneBody": "A Maturity Called Childishness",
    "blog.cardLatestTwoDate": "2026-08-05 / Philosophy / Technology",
    "blog.cardLatestTwoTitle": "On AI, the Singularity, and Communism",
    "blog.cardLatestTwoBody": "We are not discussing politics. We are discussing the future.",
    "blog.cardHouseTitle": "Step into the House of Reflection",
    "contact.title": "Contact",
    "contact.lede": "For Bentootoo, absence is a brief rest. He will reply, whoever you are.",
    "contact.email": "Leave a note in the House of Reflection",
    "contact.github": "github.com/BenTooToo",
    "contact.backTop": "Back to top",
    "footer.note": "Built for GitHub Pages."
  },
  fr: {
    "name.display": "Bentootoo",
    "nav.home": "Accueil",
    "nav.projects": "Projets",
    "nav.blog": "Maison de réflexion",
    "nav.contact": "Contact",
    "more.menuLabel": "Plus",
    "more.donate": "Me soutenir",
    "more.devlog": "Journal de dev",
    "donate.onlyfansRegion": "(Monde)",
    "donate.afdianRegion": "(Chine)",
    "hero.titleLineOne": "Bienvenue, je suis",
    "hero.lede": "C'est ici que Bentootoo laisse les inspirations s'accumuler.",
    "card.identity": "Étudiant / Créateur vidéo / Créateur de jeux / Musicien indépendant",
    "card.email": "Email",
    "projects.title": "Projets",
    "projects.lede": "Ces derniers temps, il essaie au moins de fabriquer des choses intéressantes.",
    "projects.emptyLabel": "Projet fondateur",
    "projects.emptyTitle": "Empty Class",
    "projects.emptyBody": "Un jeu d'énigmes en réalité alternée (ARG) pour explorer les secrets cachés derrière un album de classe ordinaire.",
    "projects.emptyCta": "Cliquez sur l’image pour jouer ↗",
    "projects.emptyLinkLabel": "Jouer à Empty Class",
    "projects.towerLabel": "Projet GMTK 2026 · Thème : Countdown",
    "projects.towerTitle": "Tower Off",
    "projects.towerBody": "Notre premier projet en équipe : un jeu d’action 2D en pixel art.",
    "projects.towerCta": "Jouer sur itch.io ↗",
    "projects.towerLinkLabel": "Jouer à Tower Off sur itch.io",
    "blog.title": "Maison de réflexion",
    "blog.lede": "La Maison de réflexion relie les textes de Bentootoo et les idées de ses lecteurs par la philosophie, la musique, la création et la technologie.",
    "blog.cardLatestOneDate": "2026-08-05 / Création / Musique",
    "blog.cardLatestOneTitle": "Mon idéal",
    "blog.cardLatestOneBody": "Une maturité appelée puérilité",
    "blog.cardLatestTwoDate": "2026-08-05 / Philosophie / Technologie",
    "blog.cardLatestTwoTitle": "De l'IA, de la singularité et du communisme",
    "blog.cardLatestTwoBody": "Nous ne parlons pas de politique. Nous parlons de l'avenir.",
    "blog.cardHouseTitle": "Entrez dans la Maison de réflexion",
    "contact.title": "Contact",
    "contact.lede": "Pour Bentootoo, l'absence est un court repos. Il répondra, qui que tu sois.",
    "contact.email": "Écrire depuis la Maison de réflexion",
    "contact.github": "github.com/BenTooToo",
    "contact.backTop": "Retour en haut",
    "footer.note": "Construit pour GitHub Pages."
  }
};

const languageMap = {
  zh: "zh-CN",
  en: "en",
  fr: "fr"
};

const prefersReducedMotion = window.matchMedia("(prefers-reduced-motion: reduce)").matches;
const savedLanguage = localStorage.getItem("bentootoo-language");
const defaultLanguage = savedLanguage && translations[savedLanguage] ? savedLanguage : "zh";

function setLanguage(language) {
  const dictionary = translations[language] || translations.zh;

  document.documentElement.lang = languageMap[language] || "zh-CN";
  document.querySelectorAll("[data-i18n]").forEach((element) => {
    const key = element.dataset.i18n;
    if (dictionary[key]) {
      element.textContent = dictionary[key];
    }
  });

  document.querySelectorAll("[data-i18n-aria]").forEach((element) => {
    const key = element.dataset.i18nAria;
    if (dictionary[key]) {
      element.setAttribute("aria-label", dictionary[key]);
    }
  });

  document.querySelectorAll(".lang-button").forEach((button) => {
    const isActive = button.dataset.lang === language;
    button.classList.toggle("is-active", isActive);
    button.setAttribute("aria-pressed", String(isActive));
  });

  localStorage.setItem("bentootoo-language", language);
}

document.querySelectorAll(".lang-button").forEach((button) => {
  button.addEventListener("click", () => setLanguage(button.dataset.lang));
});

setLanguage(defaultLanguage);

const emptyClassLauncher = document.querySelector("#emptyClassLauncher");
const gameLanguageDialog = document.querySelector("#gameLanguageDialog");

if (emptyClassLauncher && gameLanguageDialog) {
  emptyClassLauncher.addEventListener("click", () => gameLanguageDialog.showModal());
  gameLanguageDialog.addEventListener("click", (event) => {
    if (event.target === gameLanguageDialog) {
      gameLanguageDialog.close();
    }
  });
}

const year = document.querySelector("#year");
if (year) {
  year.textContent = String(new Date().getFullYear());
}

const avatarCard = document.querySelector(".avatar-card");
if (avatarCard) {
  const toggleAvatarCard = () => {
    const isFlipped = avatarCard.classList.toggle("is-flipped");
    avatarCard.setAttribute("aria-pressed", String(isFlipped));
  };

  avatarCard.addEventListener("click", toggleAvatarCard);
  avatarCard.addEventListener("keydown", (event) => {
    if (event.key === "Enter" || event.key === " ") {
      event.preventDefault();
      toggleAvatarCard();
    }
  });

  avatarCard.querySelectorAll("a").forEach((link) => {
    link.addEventListener("click", (event) => event.stopPropagation());
  });
}

const moreMenu = document.querySelector(".more-menu");
const moreToggle = document.querySelector(".more-toggle");
const donateMenu = document.querySelector(".donate-menu");
const donateTrigger = document.querySelector(".donate-trigger");

if (moreMenu && moreToggle) {
  const setDonateSubmenuOpen = (isOpen) => {
    if (!donateMenu || !donateTrigger) {
      return;
    }

    donateMenu.classList.toggle("is-submenu-open", isOpen);
    donateTrigger.setAttribute("aria-expanded", String(isOpen));

    if (!isOpen) {
      donateTrigger.blur();
    }
  };

  const setMoreMenuOpen = (isOpen) => {
    moreMenu.classList.toggle("is-open", isOpen);
    moreToggle.setAttribute("aria-expanded", String(isOpen));

    if (!isOpen) {
      setDonateSubmenuOpen(false);
    }
  };

  moreToggle.addEventListener("click", (event) => {
    event.stopPropagation();
    setMoreMenuOpen(!moreMenu.classList.contains("is-open"));
  });

  moreMenu.querySelectorAll(".more-dropdown > a, .donate-submenu a").forEach((control) => {
    control.addEventListener("click", () => setMoreMenuOpen(false));
  });

  if (donateMenu && donateTrigger) {
    donateTrigger.addEventListener("click", (event) => {
      event.stopPropagation();
      setDonateSubmenuOpen(!donateMenu.classList.contains("is-submenu-open"));
    });
  }

  document.addEventListener("click", (event) => {
    if (!moreMenu.contains(event.target)) {
      setMoreMenuOpen(false);
    } else if (donateMenu && !donateMenu.contains(event.target)) {
      setDonateSubmenuOpen(false);
    }
  });

  document.addEventListener("keydown", (event) => {
    if (event.key === "Escape") {
      setMoreMenuOpen(false);
      setDonateSubmenuOpen(false);
    }
  });
}

const revealObserver = new IntersectionObserver(
  (entries) => {
    entries.forEach((entry) => {
      if (entry.isIntersecting) {
        entry.target.classList.add("is-visible");
        revealObserver.unobserve(entry.target);
      }
    });
  },
  {
    threshold: 0.18,
    rootMargin: "0px 0px -8% 0px"
  }
);

document.querySelectorAll("[data-reveal]").forEach((element) => revealObserver.observe(element));

function updateProgress() {
  const scrollable = document.documentElement.scrollHeight - window.innerHeight;
  const progress = scrollable > 0 ? (window.scrollY / scrollable) * 100 : 0;
  document.documentElement.style.setProperty("--progress", `${Math.min(progress, 100)}%`);
}

const siteHeader = document.querySelector(".site-header");
const backToTop = document.querySelector(".back-to-top");
let lastScrollY = window.scrollY;

function updateHeaderVisibility() {
  if (!siteHeader) {
    return;
  }

  const currentScrollY = window.scrollY;
  const scrollingDown = currentScrollY > lastScrollY;
  const isNearTop = currentScrollY < 80;
  const changedEnough = Math.abs(currentScrollY - lastScrollY) > 4;

  if (isNearTop) {
    siteHeader.classList.remove("is-hidden");
  } else if (changedEnough && scrollingDown) {
    siteHeader.classList.add("is-hidden");
  } else if (changedEnough) {
    siteHeader.classList.remove("is-hidden");
  }

  lastScrollY = Math.max(currentScrollY, 0);
}

function updateBackToTopVisibility() {
  if (!backToTop) {
    return;
  }

  const distanceFromBottom = document.documentElement.scrollHeight - window.innerHeight - window.scrollY;
  backToTop.classList.toggle("is-visible", distanceFromBottom < 180);
}

window.addEventListener("scroll", () => {
  updateProgress();
  updateHeaderVisibility();
  updateBackToTopVisibility();
}, { passive: true });
window.addEventListener("resize", () => {
  updateProgress();
  updateBackToTopVisibility();
});
updateProgress();
updateHeaderVisibility();
updateBackToTopVisibility();

const canvas = document.querySelector("#heroCanvas");
const context = canvas ? canvas.getContext("2d") : null;
const pointer = { x: 0.5, y: 0.5 };

if (canvas && context && !prefersReducedMotion) {
  let width = 0;
  let height = 0;
  let dpr = 1;
  let time = 0;

  function resizeCanvas() {
    dpr = Math.min(window.devicePixelRatio || 1, 2);
    width = canvas.clientWidth;
    height = canvas.clientHeight;
    canvas.width = Math.floor(width * dpr);
    canvas.height = Math.floor(height * dpr);
    context.setTransform(dpr, 0, 0, dpr, 0, 0);
  }

  function drawRibbon(offset, color, amplitude, speed) {
    context.beginPath();
    const influenceX = (pointer.x - 0.5) * 90;
    const influenceY = (pointer.y - 0.5) * 70;

    for (let x = -40; x <= width + 40; x += 18) {
      const normalized = x / Math.max(width, 1);
      const waveOne = Math.sin(normalized * 8 + time * speed + offset) * amplitude;
      const waveTwo = Math.cos(normalized * 15 - time * speed * 0.7 + offset) * (amplitude * 0.34);
      const y = height * 0.5 + waveOne + waveTwo + influenceY + Math.sin(time * 0.6 + offset) * 34;
      const finalX = x + influenceX * Math.sin(normalized * Math.PI);

      if (x === -40) {
        context.moveTo(finalX, y);
      } else {
        context.lineTo(finalX, y);
      }
    }

    context.strokeStyle = color;
    context.lineWidth = 1.7;
    context.stroke();
  }

  function drawMesh() {
    context.clearRect(0, 0, width, height);
    context.globalCompositeOperation = "source-over";
    context.fillStyle = "rgba(16, 17, 15, 0.16)";
    context.fillRect(0, 0, width, height);

    context.globalCompositeOperation = "lighter";
    drawRibbon(0, "rgba(25, 211, 197, 0.48)", 92, 0.72);
    drawRibbon(1.8, "rgba(200, 255, 89, 0.36)", 72, 0.58);
    drawRibbon(3.4, "rgba(255, 106, 87, 0.3)", 112, 0.46);

    context.globalCompositeOperation = "source-over";
    context.strokeStyle = "rgba(246, 247, 242, 0.08)";
    context.lineWidth = 1;

    const columns = 9;
    const rows = 5;
    for (let col = 0; col <= columns; col += 1) {
      const x = (width / columns) * col + Math.sin(time * 0.35 + col) * 10;
      context.beginPath();
      context.moveTo(x, 0);
      context.lineTo(x + Math.sin(time * 0.5 + col) * 18, height);
      context.stroke();
    }

    for (let row = 0; row <= rows; row += 1) {
      const y = (height / rows) * row + Math.cos(time * 0.42 + row) * 8;
      context.beginPath();
      context.moveTo(0, y);
      context.lineTo(width, y + Math.cos(time * 0.3 + row) * 18);
      context.stroke();
    }
  }

  function tick() {
    time += 0.016;
    drawMesh();
    requestAnimationFrame(tick);
  }

  window.addEventListener("resize", resizeCanvas);
  window.addEventListener("pointermove", (event) => {
    pointer.x = event.clientX / Math.max(window.innerWidth, 1);
    pointer.y = event.clientY / Math.max(window.innerHeight, 1);
  });

  resizeCanvas();
  tick();
} else if (canvas && context) {
  canvas.style.display = "none";
}
